const initialState = {
  firstName: 'Benjamin',
  lastName: 'Harrison',
  email: 'b.harrison@president.us',
  password: 'harryB4Prez'
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;